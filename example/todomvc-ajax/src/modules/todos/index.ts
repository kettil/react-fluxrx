import * as actions from './actions';
import * as selectors from './selectors';
import { constants } from './types';

export const todosAction = actions;
export const todosSelectors = selectors;
export const todosConstants = constants;
